# CUDA 지원 PyTorch 설치용 requirements 파일
# 
# 사용 방법:
# 1. 먼저 시스템에 CUDA Toolkit이 설치되어 있어야 합니다 (선택사항, PyTorch가 자체 CUDA 런타임 포함)
# 2. PyTorch 공식 사이트에서 CUDA 버전 확인: https://pytorch.org/get-started/locally/
# 3. 아래 명령어로 CUDA 버전 PyTorch 설치:
#
# CUDA 12.1 예시:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# CUDA 11.8 예시:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# 또는 PyTorch 공식 사이트에서 생성된 명령어 사용

# FastAPI
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.10.4
python-multipart>=0.0.6

# WebSocket (버전 명시 필수)
websockets==12.0

# Env
python-dotenv

# AI Models (최신 Python 호환성을 위해 버전 제한 해제 권장)
transformers>=4.38.0
peft
accelerate
bitsandbytes  # 8-bit 양자화를 위한 패키지 (CUDA 필수)

# PyTorch는 위의 pip install 명령어로 별도 설치 필요
# torch
# torchaudio
# torchvision

# Test
pytest
httpx

