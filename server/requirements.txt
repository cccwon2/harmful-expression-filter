# FastAPI
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.10.4
python-multipart>=0.0.6

# WebSocket (버전 명시 필수)
websockets==12.0

# Env
python-dotenv

# AI Models (최신 Python 호환성을 위해 버전 제한 해제 권장)
transformers>=4.38.0
peft
accelerate
bitsandbytes  # 8-bit 양자화를 위한 패키지 (성능 개선)

# PyTorch (안정적인 버전 조합 사용, versions must match)
# Note: CPU version is installed. For CUDA, use: pip install torch==2.4.1+cu121 torchvision==0.19.1+cu121 torchaudio==2.4.1+cu121 --index-url https://download.pytorch.org/whl/cu121
torch==2.4.1
torchaudio==2.4.1
torchvision==0.19.1

# Test
pytest
httpx