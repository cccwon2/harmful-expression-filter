# FastAPI
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.10.4
python-multipart>=0.0.6

# WebSocket (버전 명시 필수)
websockets==12.0

# Env
python-dotenv

# AI Models (최신 Python 호환성을 위해 버전 제한 해제 권장)
transformers>=4.38.0
peft
accelerate
bitsandbytes  # 8-bit 양자화를 위한 패키지 (성능 개선)

# PyTorch (torch 2.6+ required for safetensors support, versions must match)
torch>=2.6.0
torchaudio>=2.6.0
torchvision>=0.21.0

# Test
pytest
httpx