# FastAPI 및 웹 서버
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.10.4
python-multipart==0.0.6

# WebSocket 클라이언트 (Deepgram WebSocket 연결용)
websockets

# 환경 변수 관리
python-dotenv==1.0.0

# Transformers & LoRA 모델
# (peft 최신 버전과의 호환성을 위해 transformers 버전 제한을 풀었습니다)
transformers
peft
accelerate

# PyTorch
# (Windows/Linux 환경에 맞는 최신 버전을 자동으로 설치하도록 버전 명시 제거)
torch
torchaudio
torchvision

# 테스트 (개발 환경)
pytest==7.4.3
httpx==0.25.2