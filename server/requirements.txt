# FastAPI & Web
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.10.4
python-multipart>=0.0.6
websockets==12.0

# Env
python-dotenv

# Database
supabase>=2.0.0
gotrue>=1.0.0

# AI Models (CPU Optimized)
transformers>=4.38.0
accelerate
# bitsandbytes  <-- CPU 호환 불가로 제거
# peft          <-- 학습(Fine-tuning)이 아닌 추론(Inference)만 한다면 굳이 필요 없음 (유지해도 무방)

# PyTorch (CPU 버전 설치 권장)
torch
torchaudio      # 음성 데이터 처리 필요 시 유지, 아니면 제거 권장
# torchvision   # KoElectra와 무관하므로 제거 권장

# Test
pytest
httpx

# Optional: CPU Inference Speedup (추천)
optimum         # Hugging Face 모델을 CPU/ONNX로 최적화
onnxruntime     # CPU 추론 가속용