# FastAPI 및 웹 서버
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.10.4
python-multipart==0.0.6

# WebSocket 클라이언트 (Deepgram WebSocket 연결용)
websockets

# 환경 변수 관리
python-dotenv==1.0.0

# Transformers & LoRA 모델 (Python 3.10 지원)
transformers==4.35.0
peft
accelerate

# PyTorch (Transformers 의존성)
torch==2.1.0

# 테스트 (개발 환경)
pytest==7.4.3
httpx==0.25.2